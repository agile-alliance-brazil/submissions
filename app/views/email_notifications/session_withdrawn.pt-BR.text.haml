O(s) autor(es) da sessão "#{@session.name}" (ID #{@session.id} - #{session_url(@session.conference, @session, locale: 'en')}) negaram sua participação na #{@conference_name}.


Precisamos de uma proposta reserva.


Sinceramente,

Sistema de submissão
