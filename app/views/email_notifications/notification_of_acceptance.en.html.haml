%p Dear #{h(@session.authors.map(&:full_name).join(" & "))},

%p We are pleased to inform you that your proposal <strong>'#{h(@session.title)}'</strong> has been selected to be part of the program of the Brazilian Conference about Agile Methods – #{@conference_name}!

%p
  Please refer to the comments and suggestions made by the reviewers by accessing:
  %br
  = link_to session_reviews_url(@conference, @session, :type => :final, :locale => 'en'), session_reviews_url(@conference, @session, :type => :final, :locale => 'en')

%p
  We would like to ask you to confirm your attendance at the event until #{@conference.author_confirmation.strftime('%H:%M of %B %dth, %Y')} by accessing the form at
  %br
  = link_to session_confirm_url(@conference, @session, :locale => 'en'), session_confirm_url(@conference, @session, :locale => 'en')
  %br
  Note that reviewers and track organizers may have added suggestions of changes in your proposal. If so, you will only be able to make those changes when completing the form mentioned above. Please read the comments and suggestions before completing this form so you can make all changes suggested.

%p
  If you cannot attend the event, we kindly ask you to confirm your withdrawal using the form at
  %br
  = link_to session_withdraw_url(@conference, @session, :locale => 'en'), session_withdraw_url(@conference, @session, :locale => 'en')

%p
  - if(@session.authors.size > 1)
    Any of the two authors (#{@session.author.full_name} or #{@session.second_author.full_name}) may confirm or withdraw the attendance.
  Once the attendance is confirmed or withdrawn, the decision is final and can not be changed.

%p The sessions whose authors do not confirm or withdraw their attendance until #{@conference.author_confirmation.strftime('%B %dth, %Y')} will be automatically removed from the program and replaced immediately by other selected work.

%p
  If your attendance is confirmed, the organization of #{@conference_name} will contact you about the complementary registration earned by proposal for the event.
  - if @session.authors.size > 1
    Note that, although you have two presenters for your proposal, there is only one complementary registration per proposal. When the organization contacts you, you will have to chose which presenter will use the registration. The other one will have to register as a regular attendee.

%p Should you have any queries, requests or comments, please contact the Program Committee at #{mail_to 'programa@agilebrazil.com'}.

%p
  We hope to see you soon!
  %br
  = "--"
  %br
  Program Committee of #{@conference_name} Conference

%small [Please do not reply to this e-mail. It was sent from an unmonitored email address]
