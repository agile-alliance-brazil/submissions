- title t('title.signup')
.auto_focus

= semantic_form_for resource, :as => resource_name, :url => registration_path(resource_name) do |form|
  = render 'form', :form => form
  = form.actions do
    = form.action :submit, :label => :signup, :button_html => {:tabindex => autotab}
