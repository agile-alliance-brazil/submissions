- title t('title.edit_session')
- show_actions!
.auto_focus
  
= semantic_form_for(@session) do |form|
  = render 'form', :form => form
  = form.buttons do
    = form.commit_button :edit_session, :button_html => {:tabindex => autotab}