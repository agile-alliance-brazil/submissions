Caro#{@session.authors.size > 1 ? "s":""} #{@session.authors.map(&:full_name).join(" & ")},


Devido a um problema em nosso servidor, nem todas as notificações por email foram enviadas. Para evitar mais problemas, decidimos reenviar todas as notificações. Se você já recebeu este email, por favor, desconsidere-o.


É com grande satisfação que comunicamos que seu trabalho '#{h(@session.title)}' foi selecionado para integrar o programa da Conferência Brasileira sobre Métodos Ágeis de Desenvolvimento de Software – #{@conference_name}!


Por favor, consulte as críticas e sugestões feitas pelos avaliadores acessando o endereço:

= session_reviews_url(@conference, @session, :locale => 'pt')


Por gentileza, queira confirmar sua presença no evento até às #{@conference.author_confirmation.strftime('%Hh%Mmin do dia %d/%m/%Y')} acessando o formulário

= session_confirm_url(@conference, @session, :locale => 'pt')


Caso você não possa comparecer ao evento, pedimos a gentileza de confirmar sua desistência através do formulário

= session_withdraw_url(@conference, @session, :locale => 'pt')


- if(@session.authors.size > 1)
  Qualquer um dos dois autores (#{@session.author.full_name} ou #{@session.second_author.full_name}) pode confirmar ou cancelar a proposta. Uma vez que a proposta for confirmada ou cancelada por qualquer autor, nenhum deles poderá mudar essa decisão.


A sessão que não for confirmada ou cancelada até o dia #{@conference.author_confirmation.strftime('%d/%m/%Y')} será automaticamente removida da programação, sendo substituída imediatamente por outro trabalho selecionado.


- if(@session.authors.size > 1)
  Caso a sessão seja confirmada, ambos autores serão beneficiados de isenção de inscrição.
- else
  Caso a sessão seja confirmada, você será beneficiado de isenção de inscrição.
Depois que a confirmação for realizada, a organização da #{@conference_name} entrará em contato para dar início ao processo de inscrição.


Em caso de dúvidas, pedidos ou comentários, contate a Comissão de Programa através do email programa@agilebrazil.com.


Esperamos vê-lo#{@session.authors.size > 1 ? "s":""} em breve!

#{"--"}

Equipe de programa da #{@conference_name}


[Por favor não responda este e-mail. Ele foi enviado através de um endereço não monitorado]
